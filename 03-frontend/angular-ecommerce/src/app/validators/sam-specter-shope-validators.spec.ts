import { SamSpecterShopeValidators } from './sam-specter-shope-validators';

describe('SamSpecterShopeValidators', () => {
  it('should create an instance', () => {
    expect(new SamSpecterShopeValidators()).toBeTruthy();
  });
});
